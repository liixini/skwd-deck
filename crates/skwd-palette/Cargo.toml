[package]
name = "skwd-palette"
version.workspace = true
edition = "2024"
license.workspace = true
description = "Dependency-light palette quantization + Material-style theme derivation"

[features]
material = ["dep:material-colors"]

[dependencies]
serde_json = "1"
material-colors = { version = "0.4.2", default-features = false, features = ["std", "serde"], optional = true }

[lints]
workspace = true
