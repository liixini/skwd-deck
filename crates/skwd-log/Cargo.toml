[package]
name = "skwd-log"
version = "1.0.0-beta.3"
edition = "2024"
license.workspace = true

[features]
facade = ["dep:log", "dep:libc"]
alloc = []
alloc-tap = ["alloc"]
proc = []

[dependencies]
log = { version = "0.4", optional = true }
libc = { version = "0.2", optional = true }

[lints]
workspace = true
